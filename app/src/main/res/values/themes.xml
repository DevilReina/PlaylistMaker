<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Базовая тема приложения -->
    <style name="Base.Theme.PlaylistMaker" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnBackground">@color/background</item>
        <item name="colorOnSecondary">@color/main_bg</item>
        <item name="colorOnPrimary">@color/searth_clr</item>
        <item name="colorControlNormal">@color/navigation_icon_color</item>


        <!-- Цвета статус-бара -->
        <item name="android:statusBarColor">@color/statusBarColorLight</item>
        <item name="android:windowLightStatusBar">true</item> <!-- Включает светлый статус-бар -->
    </style>

    <!-- Основная тема приложения -->
    <style name="Theme.PlaylistMaker" parent="Base.Theme.PlaylistMaker" />

    <!-- Кастомная тема для экрана с измененным статус-баром -->
    <style name="Theme.PlaylistMaker.MainScreen" parent="Base.Theme.PlaylistMaker">
        <item name="android:statusBarColor">@color/main_bg</item> <!-- Цвет статус-бара для главного экрана -->
        <item name="android:windowLightStatusBar">false</item> <!-- Темный текст в статус-баре -->
    </style>
</resources>
